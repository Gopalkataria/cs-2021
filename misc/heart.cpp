#include <stdio.h>
void main()
{
    // # number of lines  l  to print
    int l = 31;
    // # characters to print
    char c = "█", emptychar = "\s";
    // # correction parameters of the heart equations
    int k = l / 6,
        a = 0.2, b = 0.5;

    return 0;
}